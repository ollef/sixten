data Unit = unit

data Tuple a b = tuple a b

Vector : Int -> Type -> Type
Vector 0 _ = Unit
Vector n a = Tuple a (Vector (subInt n 1) a)

data Array a where
  array : forall n. Ptr (Vector n a) -> Array a

data String = String (Array Byte)
