id : forall {a}. a -> a
id x = x

id' x = x

idT : Type {_} -> Type {_}
idT t = t

idf : forall {f}{a}. f a -> f a
idf x = x

const : forall {a}{b}. a -> b -> a
const x y = x

idConst1 : forall {f}{a}. f a -> f a
idConst1 x = const x (\(x : Size). x)

useConst : forall {a}{b}. ((a -> a -> a) -> b) -> b
useConst f = f const

fun : Type {_} -> Type {1}
fun a = a -> a

a : forall {a}. a
a = a

main1 = printSize (const 10 20)
